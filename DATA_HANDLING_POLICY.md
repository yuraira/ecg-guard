# ECG Guard 데모 데이터 처리 정책

시행일: 2026-08-07

## 적용 범위

ECG Guard 웹 데모는 공개 심전도 데이터의 연구·교육 및 대회 시연을 위한
프로토타입이다. 실제 환자의 진단, 선별, 치료 또는 응급 의사결정을 지원하는
의료 서비스가 아니다.

현재 공개 데모가 허용하는 입력은 사용자가 처리 권한을 가진 다음 데이터로
한정한다.

- 공개된 연구용 WFDB 심전도
- 성명, 환자번호, 생년월일, 검사일자 등 개인을 식별할 수 있는 정보가
  제거되었거나 적절하게 비식별 처리된 연구 데이터

실제 환자의 개인정보·진료정보가 포함된 파일, 처리 권한이 없는 파일 및 임상
의사결정에 사용할 파일은 업로드할 수 없다.

## 처리 목적과 항목

업로드한 WFDB `.hea` 및 `.dat` 파일은 한 건의 12유도 심전도에 대한 연구용
모델 추론과 결과 시각화에만 사용한다. 업로드 파일을 모델 재학습, 사용자 추적,
광고 또는 제3자 제공에 사용하지 않는다.

서비스는 분석에 필요한 파형과 다음 기술 정보를 처리한다.

- 표본 주파수, 표본 수, 유도 순서 및 진폭
- 모델의 클래스별 확률, 기술 품질 지표 및 불확실성
- 체크포인트 버전과 SHA-256

공개 데모의 `합성 샘플`은 사용자의 파일을 받지 않고 실행 중 코드로 생성하는
UI 확인용 신호다. 실제 환자 기록이나 임상적으로 검증된 ECG 사례가 아니며
저장하지 않는다.

업로드 파일의 원래 이름은 결과 기록에 저장하지 않고 `uploaded-record`로
대체한다.

## 저장, 보유 및 삭제

- 업로드 원본은 요청마다 생성되는 격리된 임시 디렉터리에 기록한다.
- 원본 임시 파일은 분석 성공 또는 실패 후 요청 처리가 끝나면 삭제한다.
- 분석된 파형과 결과는 현재 Streamlit 세션 메모리에만 유지한다.
- 분석이 끝나면 업로더의 파일 상태를 초기화한다.
- 사용자는 `세션의 분석 결과 삭제` 버튼으로 파형과 결과를 즉시 제거할 수 있다.
- 서버 데이터베이스, 영구 파일 저장소 또는 모델 학습 데이터에는 저장하지 않는다.

브라우저, 프록시 및 호스팅 사업자가 처리하는 접속 IP와 네트워크 로그는 실제
배포 환경의 별도 처리방침을 따른다. 현재 호스팅 사업자, 로그 항목, 보유 기간과
국외 처리 범위는 아래에 명시한다.

## 현재 배포 및 로그 기록

2026-08-10부터 Render에서 연구·교육용 공개 서비스를 운영한다. 배포 로그에서
고정 체크포인트 검증과 `0.0.0.0:8501` 서버 기동을 확인했고, 외부 HTTPS
헬스체크·합성 ECG 분석·세션 삭제·잘못된 입력 오류 처리를 검증했다.

| 항목 | 현재 운영 환경 |
|---|---|
| 호스팅 사업자·리전 | Render Services, Inc. · Singapore 리전 · Free Web Service |
| 공개 URL·HTTPS | `https://ecg-guard.onrender.com` · Render 로드 밸런서에서 TLS 종료 및 HTTP→HTTPS 전환 · 2026-08-10 루트와 헬스체크 HTTP 200 확인 |
| 사업자 접속 로그 | 요청 시각, HTTP 메서드, 경로, 상태 코드, 응답 시간, 호스트, 클라이언트 IP, 사용자 에이전트 등 Render가 제공하는 HTTP 요청 메타데이터 |
| 사업자 로그 보유 기간 | Hobby 워크스페이스 7일. 기간이 지난 로그는 Render에서 조회할 수 없음 |
| 애플리케이션 로그 | 실행·오류 범주와 체크포인트 준비 상태만 표준 출력. IP, 업로드 파일명, WFDB 헤더 및 원문 파형은 앱이 직접 기록하지 않음 |
| 파일·결과 보유 | 임시 파일시스템과 세션 메모리만 사용. 재시작·재배포·유휴 종료 시 파일시스템 변경사항 소실. 별도 DB·영구 디스크 없음 |
| 쿠키 | Streamlit 세션 동작에 필요한 범위 외 추적·광고 쿠키를 앱에서 설정하지 않음 |
| 국외 이전 | 접속 메타데이터와 사용자가 전송한 비식별 연구 데이터가 Singapore 리전 및 Render의 서비스 제공 인프라에서 처리될 수 있음 |
| 삭제·문의 | 앱 세션 삭제 버튼으로 결과를 제거. 사업자 로그는 7일 보유기간 만료 후 조회 불가하며 기타 권리 요청은 Render 개인정보처리방침 절차를 따름 |

Render Hobby 로그 보유기간과 요청 로그 필드, HTTPS 처리 방식은 각각
[Render 로그 문서](https://render.com/docs/logging)와
[Render 웹 서비스 문서](https://render.com/docs/web-services)를 기준으로 작성했다.
무료 웹 서비스의 파일시스템은 일시적이며 15분간 요청이 없으면 종료되는
제약은 [Render 무료 서비스 문서](https://render.com/docs/free)에 따른다.

사업자, 리전, URL, 로그 항목·보유 기간, 삭제 방법 또는 국외 처리 범위가 바뀌면
이 표와 변경 시행일을 함께 갱신한다.

## 안전조치

- `.hea`와 `.dat` 형식만 허용하고 업로드 전체 크기를 50MB로 제한한다.
- 파일명의 경로·제어 문자와 중복 이름을 거부한다.
- 헤더가 선언한 100Hz, 10초, 표준 12유도 및 데이터 파일 참조를 확인한다.
- 오류 및 애플리케이션 로그에 원문 파형, 헤더 내용 또는 업로드 파일명을
  기록하지 않는다.
- 공개 통신에는 HTTPS를 사용하고 체크포인트 SHA-256을 추론 전에 검증한다.

이 조치는 실제 환자 의료정보를 처리하기 위한 인증, 접근통제, 감사로그 및
법적 동의 체계를 대신하지 않는다.

## 결과의 한계

출력은 공개 연구 데이터에서 평가한 모델의 연구용 결과이다. 의료적 진단,
질병 위험도, 치료 권고 또는 의료인의 판단을 의미하지 않는다. 외부 병원,
장비 및 전향적 임상 환경에서 검증되지 않았다.

## 문의와 보안 보고

개인정보나 환자 데이터를 공개 GitHub Issue에 첨부하지 않는다. 보안 또는
데이터 처리 문제는 저장소의
[GitHub 비공개 보안 권고](https://github.com/yuraira/ecg-guard/security/advisories/new)
기능으로 보고한다.
